import { makeStyles } from "@material-ui/core";

const useIconStyles = makeStyles(() => ({
    icon: {
        display: 'inline',
        position: 'relative'
    },
}));

export default useIconStyles;